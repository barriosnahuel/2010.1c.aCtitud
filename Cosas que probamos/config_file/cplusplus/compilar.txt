g++ configfile.cpp configuration.cpp -o configfile

uso:

./configfile
    carga el archivo por defecto

./configfile confi2.conf
    carga el archivo pasado como parámetro

./configfile  196.154.200.11 5750
    carga parámetros pasados

